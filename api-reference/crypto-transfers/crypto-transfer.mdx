---
title: 'Crypto Transfer'
openapi: 'POST /api/v1/transfers/crypto'
description: 'Submit a customer-funded USDC or USDT transfer on Solana'
---

## Request

<ParamField header="x-api-key" type="string" required>
  Active production API key belonging to the customer and source wallet merchant.
</ParamField>

<ParamField body="customerId" type="string" required>
  UUID of the customer making the transfer
</ParamField>

<ParamField body="recipient" type="string" required>
  Recipient wallet address
</ParamField>

<ParamField body="network" type="string" required>
  The blockchain network for the transfer. The only supported value is `solana`.
</ParamField>

<ParamField body="currency" type="string" required>
  The cryptocurrency to transfer. Must be one of: `usdc`, `usdt`
</ParamField>

<ParamField body="amount" type="number" required>
  Gross token amount. The configured fee is deducted and the net amount is sent to the recipient.
</ParamField>

### Request Example

```bash
curl -X POST https://api-production.fossapay.com/api/v1/transfers/crypto \
  -H "x-api-key: $FOSSAPAY_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "customerId": "11111111-1111-4111-8111-111111111111",
    "recipient": "7EcJ1eB6J3ggRrGkQWsT4ZP8dkqPZ4JmW4YRwR7Qp5cY",
    "network": "solana",
    "currency": "usdc",
    "amount": 100.50
  }'
```

```javascript
const response = await fetch('https://api-production.fossapay.com/api/v1/transfers/crypto', {
  method: 'POST',
  headers: {
    'x-api-key': process.env.FOSSAPAY_API_KEY,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    customerId: '11111111-1111-4111-8111-111111111111',
    recipient: '7EcJ1eB6J3ggRrGkQWsT4ZP8dkqPZ4JmW4YRwR7Qp5cY',
    network: 'solana',
    currency: 'usdc',
    amount: 100.50
  })
});

if (!response.ok) {
  throw new Error(`Request failed: ${response.status} ${response.statusText}`);
}

const transfer = await response.json();
```

## Response

<ResponseExample>
```json Success Response
{
  "status": true,
  "message": "processing crypto transaction",
  "data": {
    "token": {
      "symbol": "usdc",
      "amount": 100.50
    },
    "network": "SOLANA",
    "recipientAddress": "7EcJ1eB6J3ggRrGkQWsT4ZP8dkqPZ4JmW4YRwR7Qp5cY",
    "senderAddress": "So11111111111111111111111111111111111111112",
    "status": "processing"
  }
}
```
</ResponseExample>

<ResponseExample>
```json Error Response (400)
{
  "status": "error",
  "statusCode": 400,
  "message": "Crypto transfer failed. Try again later!"
}
```
</ResponseExample>

<ResponseExample>
```json Error Response (500)
{
  "status": "error",
  "statusCode": 500,
  "message": "Internal server error"
}
```
</ResponseExample>

The response indicates provider processing, not final blockchain settlement. This endpoint has no merchant-provided idempotency key or reference. After a timeout, inspect wallet history and balances before submitting another transfer.

## Errors

| Status | Typical cause |
|---|---|
| `400` | Customer/wallet unavailable, insufficient funds, unsupported operation, or provider rejection |
| `401` | Missing or invalid API key |
| `422` | Invalid customer UUID, currency, amount, recipient, or network other than `solana` |
| `5xx` | Temporary platform or provider failure; reconcile before retrying |
